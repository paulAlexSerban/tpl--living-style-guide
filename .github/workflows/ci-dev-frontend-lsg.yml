---
name: Develop Frontend Living Style Guide

on:
  push:
  pull_request:

jobs:
  dump_contexts_to_log:
    runs-on: ubuntu-latest
    steps:
      - run: echo "event name is:" ${{ github.event_name }} 
      - run: echo "event type is:" ${{ github.event.action }} 

  # dev-fe-lsg:
  #   name: Develop Frontend Living Style Guide
  #   runs-on: ubuntu-latest
  #   steps:
      # - name: "☁️ checkout repository"
      #   uses: actions/checkout@v3
      #   with:
      #     fetch-depth: 0

      # - name: "🔍  Verify Changed Frontend Living Style Guide Files"
      #   uses: tj-actions/changed-files@v23.1
      #   id: changed-frontend-living-style-guide-files
      #   with:
      #     files: |
      #         frontend/living-style-guide/**/*

      # - name: "Cache node_modules"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   uses: actions/cache@v1
      #   with:
      #     path: ~/.npm
      #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      #     restore-keys: |
      #       ${{ runner.os }}-node-

      # - name: "🔧  Setup NodeJS 14.19.1"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   uses: actions/setup-node@v1
      #   with:
      #     node-version: 14.19.1

      # - name: "🔧  Install npm@latest"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   run: npm i -g npm@latest

      # - name: "📦  Install Frontend Living Style Guide Development dependencies"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   run: bash scripts/frontend-install-living-style-guide.bash

      # - name: "🔍  Lint Frontend Living Style Guide"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   run: bash scripts/frontend-lint-living-style-guide.bash

      # - name: "🧪  Test Frontend Living Style Guide"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   run: bash scripts/frontend-test-living-style-guide.bash

      # - name: "🏗️  Build Frontend living-style-guide"
      #   if: steps.changed-frontend-living-style-guide-files.outputs.any_changed == 'true'
      #   run: bash scripts/frontend-build-living-style-guide.bash